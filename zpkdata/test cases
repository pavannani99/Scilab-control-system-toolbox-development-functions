Yes.
Use these Test Cases to get the correct answer as that of Octave.
-----------------------------------------------------------------

# Test Case 1:

s = poly(0, "s");
sys = syslin("c", (s + 2)/(s^2 + 4*s + 3));
[z, p, k, tsam] = zpkdata(sys, "v");
disp("Zeros:");
disp(z);
disp("Poles:");
disp(p);
disp("Gain:");
disp(k);
disp("Sampling time:");
disp(tsam);
-----------

# Test Case 2:

s = poly(0, "s");
sys = syslin("c", 5*(s + 4)/(s^2 + 8*s + 12));
[z, p, k, tsam] = zpkdata(sys);
disp("Zeros:");
disp(z(1));
disp("Poles:");
disp(p(1));
disp("Gain:");
disp(k);
disp("Sampling time:");
disp(tsam);
-----------

# Test Case 3:

q = poly(0, "q");
sysd = syslin(0.1, (q + 0.5)/(q^2 - q + 0.16));
[z, p, k, tsam] = zpkdata(sysd, "v");
disp("Zeros:");
disp(z);
disp("Poles:");
disp(p);
disp("Gain:");
disp(k);
disp("Sampling time:");
disp(tsam);
-----------

# Test Case 4:

A = [-1 0; 0 -2];
B = [1; 1];
C = [1 1];
D = [0];
sys = syslin("c", A, B, C, D);
[z, p, k, tsam] = zpkdata(sys);
disp("Zeros:");
disp(z(1));
disp("Poles:");
disp(p(1));
disp("Gain:");
disp(k);
disp("Sampling time:");
disp(tsam);
-----------

# Test Case 5:

sysg = [2 3; 4 5];
[z, p, k, tsam] = zpkdata(sysg);
disp("Zeros:");
disp(z(1));
disp("Poles:");
disp(p(1));
disp("Gain:");
disp(k);
disp("Sampling time:");
disp(tsam);
-----------
